
<%= form_for([@article, @article.comments.build]) do |f| %>
    <% if user_signed_in? %>
   
  <p>

    <%= f.label :body %>
    <%= f.text_area :body %>
  
  </p>
  <p>
    <%= f.submit %>
  </p>
    <% end %>
<% end %>